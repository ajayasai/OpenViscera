# Exact external origin, including non-standard port if used. No path or trailing slash.
OV_ORIGIN=https://localhost
# Optional deployment-provided font path inside the container; mount it read-only.
# OV_PDF_FONT=/fonts/department-font.ttf
